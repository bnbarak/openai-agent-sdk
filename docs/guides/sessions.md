# Sessions

Sessions provide persistent memory across agent runs.

## Options

- `MemorySession` for in-memory runs.
- `SQLiteSession` for persistent storage on disk.

## Next steps

See `src/main/java/com/acoliteai/agentsdk/core/memory` for implementations and
`docs/guides/running-agents.md` for run configuration.
