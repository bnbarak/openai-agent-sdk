package com.acoliteai.agentsdk.core;

/**
 * Partial
 *
 * <p>Represents a partial (optional fields) version of a type. In TypeScript this is Partial<T>. In
 * Java, this is typically handled through builder patterns or by making fields Optional.
 *
 * <p>This is a marker interface for partial configurations.
 */
public interface Partial<T> {
  // Marker interface for partial types
}
